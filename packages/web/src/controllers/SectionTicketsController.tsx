import { FC } from "react";

import SectionTicketsView from "../views/SectionTicketsView";

const SectionTicketsController: FC = () => {
  // tickets disabled for now
  return <></>;

  return <SectionTicketsView />;
};

export default SectionTicketsController;
